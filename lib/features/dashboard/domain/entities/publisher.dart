class Publisher {
  int? gamesCount;
  int? id;
  String? imageBackground;
  String? name;
  String? slug;

  Publisher({
    this.gamesCount,
    this.id,
    this.imageBackground,
    this.name,
    this.slug,
  });
}